# Development override: build the image from local source instead of
# pulling the published one.
#
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
services:
  lumivision:
    build: .
    image: lumivision:dev
